{
  "name": "goal-manager-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 migrations apply goal-manager-db --local",
    "db:init:remote": "wrangler d1 migrations apply goal-manager-db --remote"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "hono": "^4.6.0",
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "wrangler": "^3.87.0",
    "@types/bcryptjs": "^2.4.6"
  }
}
